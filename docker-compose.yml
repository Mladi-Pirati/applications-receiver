services:
  db:
    image: postgres:latest
    container_name: applications_db
    env_file:
      - .env
    ports:
      - 5432:5432
    volumes:
      - applications_db_data:/var/lib/postgresql
    restart: on-failure

volumes:
  applications_db_data:
